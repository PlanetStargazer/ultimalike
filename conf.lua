function love.conf(t)
    t.window.title = "RPG Test"        -- The window title (string)
    t.window.icon = nil                -- Filepath to an image to use as the window's icon 
    t.window.width = 640                
    t.window.height = 400
end